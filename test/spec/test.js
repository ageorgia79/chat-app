/* global describe, it */

(function () {
    'use strict';

    describe('Give it some context', function () {
        describe('maybe a bit more context here', function () {
            it('should run here few assertions', function () {
              var answer = 2;
              expect(2).to.equal(answer);
            });
        });
    });
})();

describe('Refresh Chat', function(){

  it ('should send the message associated as delivery to the server', function(){

  })
})

describe('Click Function', function(){

  it ('should take the input from the input field and convert to a variable to be passed', function(){

    it ('should use the message constructor to create a new object', function(){

      it ('should render the message in the textappend div',function(){
        
      })
    })
  })
})

describe('the start chat', function(){

  it ('should retrieve data from the JSON', function(){

    it ('should pass over each object looking for something called message', function(){

      it ('should render the message on the page', function(){

      })
    })
  })
})